---
phase: 4
plan: 02
title: "Scheduled Recompute & Score APIs"
wave: 2
depends_on: [04-01]
requirements: [SCORE-02, SCORE-03]
files_modified:
  - backend/app/scoring/tasks.py
  - backend/app/workers/schedules.py
  - backend/app/workers/celery_app.py
  - backend/app/scoring/routes.py
  - backend/app/scoring/services.py
  - backend/app/main.py
  - backend/scripts/recompute_scores_once.py
autonomous: true
must_haves:
  - Momentum scores recalculated every 12 hours
  - Batch processing over all tracked startups
  - Score direction and delta updated each cycle
  - API endpoints for score trend and latest score state
---

<task id="2.1">
## Task: Add 12-hour score recomputation task

Create `backend/app/scoring/tasks.py`:
- `recompute_momentum_scores_12h()`
- batch iterate startups with available signals
- call scoring engine and persist updates/history
- emit run telemetry (processed/success/failed)

Register in worker schedules with 12-hour cadence.
</task>

<task id="2.2">
## Task: Expose scoring APIs

Create `backend/app/scoring/routes.py` with auth-required endpoints:
- `GET /api/scoring/startups/{startup_id}` (latest score, delta, direction)
- `GET /api/scoring/startups/{startup_id}/history` (time-series)

Include pagination/time filters for history endpoint.
</task>

<task id="2.3">
## Task: Add one-off recompute runner

Create `backend/scripts/recompute_scores_once.py`:
- executes one scoring cycle manually
- prints summary counts and distribution checks
- useful for smoke tests before enabling scheduler
</task>

<verify>
## Verification

1. 12-hour scheduler enqueues scoring recompute task
2. Manual one-off runner updates scores for tracked startups
3. API returns latest score + direction for a startup
4. History endpoint returns chronological score snapshots
5. Distribution sanity check shows non-degenerate score spread
</verify>
