---
phase: 5
plan: 02
title: "Feed UI Cards & Ranking View"
wave: 1
depends_on: [05-01]
requirements: [FEED-01, FEED-02]
files_modified:
  - frontend/src/app/(dashboard)/feed/page.tsx
  - frontend/src/components/feed-card.tsx
  - frontend/src/lib/startups.ts
autonomous: true
must_haves:
  - Feed page renders ranked startup cards from API data
  - Each card shows startup identity, sector, score, direction, and key highlights
  - Empty/loading/error states are explicit and lightweight
  - UX-polished presentation using existing design primitives only
---

<task id="2.1">
## Task: Build feed data hook and page flow

Implement feed query wiring in `frontend/src/lib/startups.ts` and consume in `feed/page.tsx`:
- load initial ranked page
- keep URL-safe query param support where already used
- show skeleton/loading and retry UI for transient failures
</task>

<task id="2.2">
## Task: Create feed card component

Create `frontend/src/components/feed-card.tsx`:
- startup name, sector/stage metadata
- momentum score prominence
- score change indicator (up/down/flat)
- compact text highlights for hiring + LinkedIn changes

Keep component reusable for future watchlist feed usage.
</task>

<task id="2.3">
## Task: Add startup detail navigation entrypoint

Card click navigates to startup detail route:
- preserve relevant list context in URL/state where practical
- ensure keyboard-accessible interaction and focus behavior
</task>

<verify>
## Verification

1. Feed page loads ranked startup list from backend
2. Card content satisfies FEED-02 field requirements
3. Card click opens startup detail page route correctly
4. Loading, empty, and error states are functional and non-blocking
</verify>
