---
phase: 5
plan: 03
title: "Feed Filters & Search Discovery"
wave: 2
depends_on: [05-01, 05-02]
requirements: [FEED-03, SRCH-01, SRCH-02, SRCH-03]
files_modified:
  - backend/app/startups/routes.py
  - backend/app/startups/services.py
  - backend/app/startups/schemas.py
  - frontend/src/app/(dashboard)/feed/page.tsx
  - frontend/src/lib/startups.ts
autonomous: true
must_haves:
  - Filters for sector, stage, and score range
  - Name search with responsive query updates
  - Results preserve momentum score and key signal visibility
  - Standard-scope API filtering with clear parameter contracts
---

<task id="3.1">
## Task: Extend backend filter/search support

Add/verify query parameters for feed/search endpoint:
- `q` for startup name search
- `sector`, `stage`, `min_score`, `max_score`
- maintain deterministic ordering by score

Update schemas/docs to reflect supported filters.
</task>

<task id="3.2">
## Task: Build filter/search controls in feed UI

Implement dashboard controls in feed page:
- search input (debounced)
- sector + stage selectors
- score range controls (simple min/max inputs or slider if already available)
- clear/reset filters action

Use existing component and style tokens only.
</task>

<task id="3.3">
## Task: Keep URL/query-state in sync

Persist filter/search state in URL query params to support:
- shareable links
- browser back/forward navigation
- predictable data refetch behavior
</task>

<verify>
## Verification

1. Search by name narrows feed results correctly
2. Sector/stage/score filters apply together
3. Clearing filters restores default ranked feed
4. Results continue to show score + key signal highlights
</verify>
