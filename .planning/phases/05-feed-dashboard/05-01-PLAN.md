---
phase: 5
plan: 01
title: "Feed Query Layer & Ranking API"
wave: 1
depends_on: [04-01, 04-02]
requirements: [FEED-01, FEED-02, SRCH-03]
files_modified:
  - backend/app/startups/services.py
  - backend/app/startups/routes.py
  - backend/app/startups/schemas.py
  - backend/app/signals/services.py
autonomous: true
must_haves:
  - Feed-ready endpoint returns startups ranked by momentum score descending
  - Card payload includes score, score change, and key signal highlight fields
  - Query supports pagination and stable sorting for dashboard use
  - Keep complexity standard and API contracts explicit for frontend consumption
---

<task id="1.1">
## Task: Build feed-focused startup query contract

Extend startup listing service to support feed usage:
- default sort: `momentum_score desc`, then `score_updated_at desc`
- include card-friendly fields (`name`, `sector`, `stage`, `momentum_score`, `score_change`, `score_direction`)
- include lightweight signal summary values for highlights

Expose via existing startups router as the primary source for feed rendering.
</task>

<task id="1.2">
## Task: Add key signal highlight shaping

In service/schema layer, shape API response with compact highlight fields:
- latest hiring signal delta summary
- latest LinkedIn growth summary
- fallback behavior when one signal is missing

Keep payload deterministic and stable for FEED card rendering.
</task>

<task id="1.3">
## Task: Ensure paginated ranking behavior

Add/verify pagination inputs and metadata:
- cursor or offset-based pagination (consistent with existing patterns)
- stable result order across page transitions
- predictable page size bounds for UI performance
</task>

<verify>
## Verification

1. Feed query returns startups sorted highest score first
2. Card payload includes score + direction + key signal highlights
3. Pagination returns stable non-duplicated slices
4. Endpoint remains auth-safe and compatible with existing startup consumers
</verify>
