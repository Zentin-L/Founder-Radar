---
phase: 3
plan: 04
title: "Signal Data API, Reliability & Backfill"
wave: 3
depends_on: [03-02, 03-03]
requirements: [SIGNAL-01, SIGNAL-02, SIGNAL-03, SIGNAL-04, SIGNAL-05]
files_modified:
  - backend/app/signals/routes.py
  - backend/app/signals/services.py
  - backend/app/signals/tasks.py
  - backend/scripts/backfill_signals.py
  - backend/scripts/signal_health_report.py
  - backend/README.md
autonomous: true
must_haves:
  - Unified query endpoints for historical signals
  - Time-series responses with pagination/filtering
  - Pipeline health/error visibility for operators
  - Backfill script for initial 30-day baseline
  - Reliability guardrails (timeouts, retries, dead-letter handling) sized for standard scope
---

<task id="4.1">
## Task: Build unified signal history API

Extend `backend/app/signals/routes.py` with:
- `GET /api/signals/{startup_id}` for merged HIRING+LINKEDIN timeline
- query params: `type`, `from`, `to`, `limit`, `cursor`
- auth-required responses for dashboard usage

Add service methods for efficient paginated reads.
</task>

<task id="4.2">
## Task: Add reliability and operational tooling

In tasks/service layer:
- dead-letter/error capture for repeated task failures
- run-level metrics: processed, success, failed, skipped
- threshold alerts in health payload when failure rate spikes

Create `backend/scripts/signal_health_report.py`:
- CLI summary for recent task runs and failure rates.
</task>

<task id="4.3">
## Task: Implement historical backfill

Create `backend/scripts/backfill_signals.py`:
- backfill signal snapshots for recent window (default 30 days)
- chunked execution per startup to avoid overload
- resumable mode via cursor/checkpoint file
- supports 12-hour snapshot intervals for backfilled windows

Document runbook in `backend/README.md`.
</task>

<verify>
## Verification

1. Unified signal endpoint returns merged, paginated time-series
2. Health report script outputs latest pipeline status successfully
3. Backfill script can run in dry-run and apply mode
4. 30-day historical rows are present for sample startups
5. Failure spikes appear in health output with clear diagnostics
</verify>
