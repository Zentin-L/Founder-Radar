---
phase: 6
plan: 06
title: "Performance QA, Device Matrix & Vercel Launch"
wave: 3
depends_on: [06-03, 06-04, 06-05]
requirements: [MKT-02, MKT-04]
files_modified:
  - frontend/README.md
  - frontend/next.config.ts
  - frontend/src/lib/marketing/perf-monitor.ts
  - frontend/src/lib/marketing/feature-flags.ts
  - .planning/STATE.md
autonomous: true
must_haves:
  - Measured performance against desktop and mobile targets
  - Feature-flag rollback for heavy 3D modules
  - Cross-device validation matrix with explicit pass/fail gates
  - Vercel-ready env/config checklist and launch runbook
---

<task id="6.1">
## Task: Implement performance guardrails and monitoring

Add runtime telemetry:
- FPS sampling and jank markers
- hydration + 3D chunk load timings
- memory trend observation for long scroll sessions

Optimization checklist:
- dynamic import all heavy 3D clusters
- geometry/material reuse and explicit dispose strategy
- LOD or section visibility culling for off-screen elements
</task>

<task id="6.2">
## Task: Run device/browser validation matrix

Required matrix:
- macOS Safari + Chrome (M1/M2)
- Windows Chrome + Edge (recent GPU)
- iPhone Safari (recent + one older)
- Android Chrome (mid-range)

Success gates:
- desktop near 60fps
- acceptable ~30fps mobile-lite
- first load under ~3s for non-3D critical content
</task>

<task id="6.3">
## Task: Launch runbook and rollback readiness

Create launch checklist:
- env vars for Resend + analytics + feature flags
- preview deployment signoff
- production monitoring + alert thresholds
- fallback switch to static hero if severe regressions occur
</task>

<verify>
## Verification

1. Performance targets are measured and documented per device tier
2. Feature flag can disable heavy 3D blocks without breaking conversion path
3. Vercel deploy completes with all required metadata and env vars
4. Launch checklist is complete and reproducible by the team
</verify>
